version '1.0'

apply plugin: 'application'
mainClassName = 'gui.MainWindow'
sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

compileJava {
    doFirst {
        exec {
            executable './1.sh'
        }
    }
}

run {
    systemProperties 'java.security.policy' : file("policy.all")
}
