version '1.0'

apply plugin: 'application'

mainClassName = 'app.ServerApp'
sourceCompatibility = 1.8


repositories {
    mavenCentral()
}

run {
   doFirst {
        exec {
            executable './1.sh'
        }
    }
	systemProperties 'java.security.policy' : file("policy.all")
}
